var NotePreviewColumnPreferences = {
  init() {
    // Preference values are bound by Zotero through each control's
    // `preference` attribute. The main plugin observes changes and redraws.
  },
};
